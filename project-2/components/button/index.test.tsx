import React from 'react';
import { render, screen } from '../../test/test-utils';

import Button from '.';

describe('Button Component', () => {
    it.todo('should render anchor component if as=prop passed')
    it.todo('should render button component if as is not passed')
    it.todo('should disables button if no country selected')
    it.todo('should enables button if country is selected')
    it.todo('should redirect to the passed href') // * have a look on next-router-mock package
})