import styled from "styled-components";

export const StyledImageUploader = styled.div`
    position: relative;
    cursor: pointer;

    .input-image {
        position: absolute;
        top: 0;
        right: 0;
        opacity: 0;
        cursor: pointer;
        width: 35px
    }
`